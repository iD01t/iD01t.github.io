document.addEventListener('DOMContentLoaded', () => {
    console.log('iD01t Store main.js loaded.');

    // Future global scripts can go here, for example:
    // - Mobile navigation toggle
    // - Cookie consent banner
    // - Analytics integration
});
